% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run.R
\name{saintexpress_run}
\alias{saintexpress_run}
\title{Run SAINTexpress on prepared input}
\usage{
saintexpress_run(
  si,
  type = c("spc", "int"),
  workdir = getwd(),
  cleanup = TRUE,
  use_docker = NULL
)
}
\arguments{
\item{si}{Named list with elements \code{inter}, \code{prey}, \code{bait} (data frames).}

\item{type}{\code{"spc"} (spectral counts) or \code{"int"} (intensities).}

\item{workdir}{Directory to write inputs and read \code{list.txt} from. Defaults
to \code{getwd()}.}

\item{cleanup}{If \code{TRUE}, remove generated files after reading the results.}

\item{use_docker}{\code{NULL} (auto: native first, Docker fallback on macOS),
\code{TRUE} to force Docker, or \code{FALSE} to require a native binary.}
}
\value{
List with elements \code{listFile} (data frame with the path),
\code{list} (the parsed \code{list.txt}), and \code{out} (data frame with the run log).
}
\description{
Writes \code{inter.txt}, \code{prey.txt}, \code{bait.txt} into \code{workdir}, then runs the
bundled native binary, or Docker on macOS when no native binary is available
(or when \code{use_docker = TRUE}).
}
\examples{
si <- list(
  inter = data.frame(
    ipId = c("IP1", "IP1", "IP2", "IP2", "IP3", "IP4"),
    baitId = c("BaitA", "BaitA", "BaitA", "BaitA", "Ctrl", "Ctrl"),
    preyId = c("Prey1", "Prey2", "Prey1", "Prey2", "Prey1", "Prey2"),
    quant = c(20, 1, 18, 1, 1, 1)
  ),
  prey = data.frame(
    preyId = c("Prey1", "Prey2"),
    preyLength = c(500, 500),
    preyGeneId = c("Gene1", "Gene2")
  ),
  bait = data.frame(
    ipId = c("IP1", "IP2", "IP3", "IP4"),
    baitId = c("BaitA", "BaitA", "Ctrl", "Ctrl"),
    CorT = c("T", "T", "C", "C")
  )
)
if (nzchar(saintexpress_executable("spc"))) {
  workdir <- tempfile("saintexpressbin-")
  dir.create(workdir)
  saintexpress_run(si, type = "spc", workdir = workdir, use_docker = FALSE)
}
}
